import { SupportRequestGuard } from './support-request.guard';

describe('SupportRequestGuard', () => {
  it('should be defined', () => {
    expect(new SupportRequestGuard()).toBeDefined();
  });
});
